# テストケース

本書は `SPEC.md`・`USECASE.md`・`SEQUENCE.md`・`CLASS.md`・`UI.md` を元に、初期版の受け入れ条件（SPEC.md 30章）を検証するテストケースを定義する。

## 1. 環境・起動

| No | 対象 | 前提条件 | 手順 | 期待結果 | 関連 |
|---|---|---|---|---|---|
| T-001 | Python仮想環境なしでの起動 | `.venv`等を作成していないWindows 11環境 | プロジェクトルートで`python .\orchestrator\orchestrator.py --once`を実行 | 仮想環境なしで正常起動する。`pip install`不要 | SPEC.md 3章 |
| T-002 | プロジェクトルートからの起動 | 標準フォルダ構成 | `python .\orchestrator\orchestrator.py`を実行 | 正常に起動し、`project-root`を正しく検出する | SPEC.md 13章 |
| T-003 | orchestratorフォルダからの起動 | 同上 | `orchestrator`フォルダへ移動し`python .\orchestrator.py`を実行 | 実行場所によらず同じ`mail`フォルダと対象プロジェクトを検出する | SPEC.md 13章, 6章 |
| T-004 | Python Launcherでの起動 | `py`コマンドが利用可能 | `py -3 .\orchestrator\orchestrator.py`を実行 | 正常に起動する | SPEC.md 13章 |
| T-005 | カレントディレクトリ非依存 | 任意の別ディレクトリから絶対パスで起動 | 無関係なディレクトリから`python C:\...\orchestrator.py`を実行 | カレントディレクトリに関わらず同じプロジェクト・メールDBを使用する | SPEC.md 6章 |
| T-006 | 対象プロジェクト不在 | `config.json`の`project_path`が存在しないパスを指す | オーケストレーターを起動 | AIを起動せず、元メール送信者へ`DELIVERY_FAILED`通知を送る | SPEC.md 6章 |
| T-007 | config.json省略時の既定値 | `config.json`の一部項目を省略 | オーケストレーターを起動 | 安全な既定値（5秒/1800秒/30秒/10回/2回/14400秒）が適用される | SPEC.md 10章 |

## 2. コピー試験（SPEC.md 28章）

| No | 対象 | 前提条件 | 手順 | 期待結果 | 関連 |
|---|---|---|---|---|---|
| T-010 | フォルダ単位コピー | 動作確認済みの`mail`フォルダを配置した空の試験用プロジェクト | `orchestrator`フォルダだけをコピーし、PowerShellで試験用プロジェクトへ移動して起動 | 仮想環境作成なしで起動し、`mail`パッケージ・`config.json`・未読メール・CLIを検出できる | SPEC.md 28章 |
| T-011 | 日本語文字化けなし（コピー後） | T-010と同一環境 | 日本語件名・本文のメールを送受信 | 文字化けしない | SPEC.md 28章, 21章 |
| T-012 | コピー元固有ファイルへの非依存 | T-010と同一環境 | ソースコードを一切修正せずに動作確認 | コピー元プロジェクト固有のモジュール・絶対パス・環境変数・設定ファイルに依存しない | SPEC.md 5章, 28章 |

## 3. メール監視・AI起動

| No | 対象 | 前提条件 | 手順 | 期待結果 | 関連 |
|---|---|---|---|---|---|
| T-020 | 未読なしでの待機 | 全AI未読0件 | 常時監視モードで一定時間待機 | AIやLLMを呼び出さずトークンを消費しない | SPEC.md 15章, 22章 |
| T-021 | 未読検出と起動 | 作業AI宛てに未読メールを1件送信 | 監視サイクルを実行 | 対応するCLIが起動される | SPEC.md 15章 |
| T-022 | 二重起動防止 | 対象AIが実行中 | 同じAI宛てに新規未読メールを追加し監視サイクルを実行 | 新しいプロセスを起動しない | SPEC.md 15章 |
| T-023 | 処理順（エージェント定義順） | 複数AIに未読メールがある状態 | 監視サイクルを実行 | `config.json`のエージェント定義順に処理される（登録UID順ではない） | SPEC.md 16章 |
| T-024 | 同一AI内のメールID順 | 同じAI宛てに複数の未読メール | AIを起動 | メールID昇順で処理される | SPEC.md 16章 |
| T-025 | 一度の起動で複数メール処理 | 対象AI宛てに未読メールが複数件 | AIを起動 | 1回の起動で複数の未読メールをまとめて処理する | SPEC.md 8章 |
| T-026 | 常時監視モードの継続 | 未読なし状態が継続 | 一定時間監視を継続 | 最大連続実行時間（既定14400秒）に達するまで稼働し続ける | SPEC.md 14章 |
| T-027 | 最大連続実行時間超過 | 最大連続実行時間を短く設定（テスト用） | 設定時間を超過させる | 安全停止する | SPEC.md 14章 |
| T-027a | 最大連続実行時間=0（無期限） | 最大連続実行時間に`0`を設定 | 既定値（14400秒）を超える時間、常時監視モードを継続 | `0`のときだけ無期限として扱われ、時間経過による安全停止が発生しない | SPEC.md 10章, 14章 |
| T-027b | 最大連続実行時間の正値境界 | 最大連続実行時間に小さな正値を設定 | 設定値の直前・直後で挙動を確認 | 設定値未満では継続し、超過時にだけ安全停止する（`0`以外は必ず有限上限として扱う） | SPEC.md 10章, 14章 |
| T-028 | 一巡モードの完了待ち | 作業AI宛てに未読メール1件 | `--once`で起動 | 起動したAIが返信を作成した後に終了する（返信前に終了しない） | SPEC.md 14章 |
| T-029 | 一巡モードでの競合時NO_WORK | 起動直前に未読が0になるよう調整 | `--once`で起動 | 返信を要求せず`NO_WORK`をログに記録して正常終了する | SPEC.md 14章 |

## 4. AI実行・成果物・QandA

| No | 対象 | 前提条件 | 手順 | 期待結果 | 関連 |
|---|---|---|---|---|---|
| T-030 | 固定指示の受け渡し | 作業AI起動対象 | AIを起動 | SPEC.md 12章の固定指示文がCLIへ渡される | SPEC.md 12章 |
| T-031 | 成果物の保存 | 作業依頼メール受信済み | AIが作業を実行 | 成果物が対象プロジェクト内へ保存され、メールに相対パスが記載される（絶対パスは含めない） | SPEC.md 18章 |
| T-032 | QandA.md新規作成 | `QandA.md`が存在しない | AIが不明点を検出 | `QandA.md`が新規作成され、質問番号・状態・重要度・質問者・対象ファイル・質問内容・推奨案を含む | SPEC.md 19章 |
| T-033 | QandA状態遷移 | `QandA.md`にOPENの質問が存在 | 指揮AIが回答、その後反映 | 状態が`OPEN→ANSWERED→APPLIED`と遷移する | SPEC.md 19章 |
| T-034 | 指揮AIの確認範囲 | `QandA.md`に複数状態の質問が混在 | 指揮AIが確認 | 原則として`OPEN`の質問だけを確認する | SPEC.md 19章 |
| T-035 | 日本語成果物の文字化けなし | 日本語を含む成果物 | 成果物を作成・保存 | 文字化けしない | SPEC.md 18章, 21章 |

## 5. 依頼ID

| No | 対象 | 前提条件 | 手順 | 期待結果 | 関連 |
|---|---|---|---|---|---|
| T-040 | 新規依頼IDの生成 | 新しい目的の作業依頼 | 指揮AIが依頼メールを作成 | `JOB-{UTC日時}-{ランダム8桁}`形式（UUID由来）の依頼IDが件名に含まれる | SPEC.md 17章 |
| T-040a | 最初の依頼メールの送信者 | AI登録済み・未読0件の初期状態 | 人間/ChatGPTが指揮AIを手動起動し、最初の作業依頼を送信させる | 送信されたメールの送信者UIDが指揮AIのUIDであり、人間側のUIDではない（人間はメールシステム上の送信者にならない） | SPEC.md 9.1, 9.3, 29章, QandA Q011 |
| T-040b | 自動処理の開始点 | 同上 | 最初の作業依頼メール送信の前後でオーケストレーターを動作させる | 送信前は未読0件のため何も起動せず（`NO_WORK`）、送信後に初めて作業AIを起動する。オーケストレーターは最初の依頼メールを代理送信しない | SPEC.md 9.1, 29章, QandA Q011 |
| T-040c | 返信先が指揮AIになること | 指揮AIが送信した作業依頼を作業AIが処理 | 完了報告・エラー通知・QandA回答の各経路を実行 | いずれも返信先が指揮AIのUIDとなる（元メールの送信者UID） | SPEC.md 9.3, 30章「通知先」, QandA Q011 |
| T-041 | 同一依頼IDの継続 | 進行中の依頼への回答・修正指示 | 回答・修正指示メールを送信 | 元と同じ依頼IDが使用される | SPEC.md 17章 |
| T-042 | 独立したやり直しでの新規ID | 元の作業とは無関係なやり直し | 新規メールを作成 | 新しい依頼IDが使用される | SPEC.md 17章 |

## 6. 引継ぎ

| No | 対象 | 前提条件 | 手順 | 期待結果 | 関連 |
|---|---|---|---|---|---|
| T-050 | 引継ぎ情報の保存 | 依頼処理が進行中 | 処理の区切りで引継ぎを保存 | `orchestrator/checkpoints/`に依頼ID・目的・状態・決定事項・成果物・未解決事項・次に行うこと・最終更新日時が保存される | SPEC.md 20章 |
| T-051 | 別セッションでの再開 | 引継ぎ情報が存在する状態でプロセス再起動 | オーケストレーター・AIを再起動 | 引継ぎ情報→未回答QandA→成果物→対象メールの順で読み、処理を継続できる | SPEC.md 20章 |
| T-052 | 引継ぎ不在時の再開 | 引継ぎファイルが存在しない状態でPC再起動 | オーケストレーターを再起動 | SQLiteメール履歴・QandA.md・成果物から未完了作業を確認できる | SPEC.md 20章, 26章 |

## 7. 障害・エラー通知

| No | 対象 | 前提条件 | 手順 | 期待結果 | 関連 |
|---|---|---|---|---|---|
| T-060 | CLI未検出 | `config.json`未設定かつPATH上にCLIなし | 対象AI宛て未読メールがある状態で監視 | 元メールの送信者UIDへ`DELIVERY_FAILED`通知が届く | SPEC.md 26章, 30章 |
| T-061 | CLI起動失敗の再試行 | CLI起動が一時的に失敗する環境 | 監視サイクルを実行 | 対象AI未受信が明らかなため最大再試行回数まで再試行し、上限到達後に通知する | SPEC.md 10章, 26章 |
| T-062 | CLI異常終了（起動成功後） | CLIの起動には成功し、その後0以外の終了コードで終了する状況を再現 | AIを起動 | 元メール送信者へ`FAILED`通知が届く。CLI起動には成功しており未受信が明らかとは言えないため、自動再試行しない（T-061の「起動失敗」とは区別する） | SPEC.md 26章 |
| T-063 | CLIタイムアウト（起動成功後） | CLIの起動には成功し、その後応答しない状況を再現。CLI実行タイムアウトを短く設定 | 起動済みCLIがタイムアウトするまで待機 | タイムアウトをログへ記録し、プロセスを終了、送信者へ`TIMEOUT`通知が届く。CLI起動には成功しているため自動再試行しない（T-061の「起動失敗」とは区別する） | SPEC.md 26章, 30章「タイムアウト時」 |
| T-064 | 正常終了だが返信なし | CLIが終了コード0で返信メールを作成しない状況 | AIを起動 | 返信確認タイムアウト後、送信者へ`NO_REPLY`通知が届く | SPEC.md 30章「返信メールの確認」 |
| T-064a | Invocation照合条件の成立 | CLIが正しい構造化結果メールを作成する状況 | AIを起動し返信確認を実行 | 送信者UID、メールID、作成時刻、および本文JSONのjob/decision/invocation/parent/root/trigger/resultがすべて一致する場合だけ終端結果と判定される | SPEC.md 30章「返信メールの確認」 |
| T-064b | Invocation照合条件を1つでも欠く結果の非採用 | 上記構造化項目の1つだけが欠落または不一致の結果メールを用意 | AIを起動し返信確認を実行 | いずれの場合も結果として採用せず`NO_REPLY`となる | SPEC.md 30章「返信メールの確認」 |
| T-064c | 古い同一依頼IDメールの除外 | 同じ依頼IDを持つ、元メールより前に作成された古いメールが存在 | AIを起動し返信確認を実行 | 古いメールを返信とみなさず`NO_REPLY`となる | SPEC.md 30章「返信メールの確認」 |
| T-064d | 第三者転送の正常終了 | Directorが別AIへ`DECISION_REQUEST`を送り、本文へ一致するInvocationメタデータと`DELEGATED`を記録 | 返信確認を実行 | 宛先が起点メール送信者でなくても`DELEGATED`として正常終了し、`NO_REPLY`にならない | SPEC.md 30章「返信メールの確認」 |
| T-064h | 構造化制御通知の非起動 | `message_type: SYSTEM_ALERT`かつ`task_eligible: false`の未読通知がAI宛てに存在 | 監視サイクルを複数回実行 | 通知は人間確認用に未読のまま残り、CLIを一度も起動せず、同じ通知を再処理しない | SPEC.md 30章「制御通知」 |
| T-064i | 件名による通常タスクの誤除外防止 | 件名に`[NO_REPLY]`を含むが本文が`task_eligible: true`の通常タスク | 監視サイクルを実行 | 件名だけで除外されず、通常どおりCLIを起動する | SPEC.md 30章「制御通知」 |
| T-064j | 明示的な非タスク種別の非起動 | `message_type`がSYSTEM_ALERT以外でも本文が`task_eligible: false`の未読メール | 監視サイクルを実行 | CLIを起動せず終端インデックスへ記録し、繰り返し処理しない | SPEC.md 30章「制御通知」 |
| T-064e | 返信確認での既読化なし | 他AI宛ての未読メールが存在する状態 | 返信確認処理を実行 | 返信確認によって他AI宛てメールの既読状態が変化しない（`find_mails`による読み取り専用照会である） | SPEC.md 7章, 30章, QandA Q012 |
| T-064f | 重複結果 | 同一Invocationへ有効な結果を複数送信 | 返信確認を実行 | 最小メールIDを正本とし、後続IDを重複として記録する |
| T-064g | タイムアウト境界の結果 | タイムアウト直後かつ有限grace期間内に正しい結果を送信 | 最終返信確認を実行 | 正しい結果が採用される。終端確定後の遅延結果は状態を再オープンしない |
| T-065 | 通知の必須項目 | T-060〜T-064のいずれかの通知 | 通知メール本文を確認 | 状態・依頼ID・元メールID・元の送信者UID・処理対象AI名・処理対象UID・失敗段階・失敗理由・CLI終了コード・実行時間・再試行回数・最終試行日時・元メールの状態・推奨する次の対応が含まれる | SPEC.md 30章「本文」 |
| T-066 | 通知の秘密情報除外 | 同上 | 通知メール本文を確認 | APIキー・認証トークン・Cookie・パスワード・秘密の環境変数・本文全文・プロンプト全文・個人情報を含む絶対パス・無制限な標準出力/標準エラーを含まない | SPEC.md 30章「エラー情報の制限」 |
| T-067 | 最大往復回数到達 | 同一依頼IDで往復回数が上限に到達 | 往復を繰り返す | 上限到達後に自動処理が停止する | SPEC.md 25章, 30章 |
| T-067a | 往復回数に含めないメール種別 | 同一依頼IDで、完了報告・エラー通知・単なる受領通知だけをやり取りする | 各種別のメールを送受信し往復回数を確認 | これらは往復回数へ計上されず、上限到達扱いにならない | SPEC.md 10章 |
| T-067b | 往復回数と再試行回数の分離 | 同一依頼IDで再試行と往復の両方が発生する状況 | 再試行を発生させたうえで往復を行う | 再試行回数の増加が往復回数へ加算されず、両者が別カウンターで管理される | SPEC.md 10章 |
| T-068 | 通知失敗時のHUMAN_REQUIRED | 通知送信自体が失敗する状況を再現 | エラー通知を送信 | 再帰通知を作らず、ログ・引継ぎへ記録し`HUMAN_REQUIRED`となる | SPEC.md 30章「エラー通知による無限ループ防止」 |
| T-069 | 無効な送信者UIDへの非通知 | 元メール送信者UIDが存在しない/削除済み | エラー通知処理を実行 | 別AIへ通知先を変更せず、`HUMAN_REQUIRED`として記録する | SPEC.md 30章 |
| T-070 | orchestrator自身への非通知 | 元メールの送信者が`orchestrator`自身 | エラー通知処理を実行 | 再帰的に送信せず、人間判断状態として記録する | SPEC.md 30章 |
| T-071 | 同一失敗通知の無限生成防止 | T-060〜T-064のいずれかが繰り返し発生する状況 | 複数回の失敗を発生させる | 同じ失敗通知が無限に生成されない | SPEC.md 30章 |
| T-072 | 送信元AIの待ち続け防止 | 対象AIが応答不能な状況 | 一連の失敗処理を実行 | 送信元AIが永久に返信待ち状態にならない（エラー通知が到達する） | SPEC.md 30章 |

## 7.5 mail検索API（`find_mails`）の利用（QandA Q012）

`mail`パッケージ側の単体テストは`mail/SPEC.md`第23.1節に定める。ここでは`orchestrator`が`find_mails`を正しく利用していることを検証する。

| No | 対象 | 前提条件 | 手順 | 期待結果 | 関連 |
|---|---|---|---|---|---|
| T-073 | 検索しても既読状態が変化しない | 対象AI宛ての未読メールが存在 | 返信確認・STALE復旧の各処理を実行 | 実行前後で`check_mail`の未読数が変化せず、対象メールが未読のまま維持される | SPEC.md 7章, 30章 |
| T-074 | 送信者UIDによる絞り込み | 複数の送信者からのメールが存在 | `sender_uid`を指定して返信確認を実行 | 指定した送信者のメールだけが照合対象となる | SPEC.md 30章 |
| T-075 | 受信者UIDによる絞り込み | 複数の受信者宛てのメールが存在 | `recipient_uid`に期待する返信先UIDを指定して返信確認を実行 | 指定した受信者宛てのメールだけが照合対象となる | SPEC.md 30章 |
| T-076 | 依頼IDによる絞り込み | 異なる依頼ID（`JOB-A`／`JOB-B`）のメールが混在 | `request_id`に`JOB-A`を指定して返信確認を実行 | 件名に`[JOB-A]`を含むメールだけが照合対象となり、`JOB-B`のメールを返信と誤認しない | SPEC.md 17章, 30章 |
| T-077 | 元メールIDより古いメールの除外 | 元メールIDと同値・それ以下のメールが存在 | `after_mail_id`に元メールIDを指定して返信確認を実行 | 元メールIDより大きいメールだけが返信として採用され、同値のメールは除外される | SPEC.md 30章 |
| T-078 | CLI起動時刻より古いメールの除外 | 同一依頼ID・送受信者UIDも一致し、**メールIDは元メールIDより大きいが、CLI起動時刻より前に作成された**メールが存在 | `sent_after`にCLI起動時刻を指定して返信確認を実行 | CLI起動後に作成されたメールだけが返信として採用される。`after_mail_id`だけでは除外できない条件のため、`sent_after`の指定漏れを検出できる | SPEC.md 30章 |
| T-079 | STALE復旧時の元メール既読状態確認 | STALE検出、元メールが未読／既読の各状態 | `find_mails`で元メールの状態を確認 | `is_read`・`read_at`から未読／既読を判定でき、`receive_mail`を使用しない（元メールを奪って既読化しない） | SPEC.md 24章 |
| T-079a | 他AI宛てメール検索が受信を妨害しない | 対象AI宛ての未読メールが存在する状態で、オーケストレーターが検索を実行 | 検索後に対象AIを起動し`receive_mail`を実行 | 対象AIが当該メールを未読として取得でき、内容が一致する。検索が受信処理を妨害しない | SPEC.md 7章, 24章, 30章 |
| T-079b | DBへの直接SQLアクセスがないこと | ソースコード一式 | `orchestrator`側のコードを検査 | `sqlite3`のimport、テーブル名・列名・DBファイルへの直接接続が存在せず、メール参照がすべて`mail`公開関数経由である | SPEC.md 7章, QandA Q012 |

## 8. 実行時情報・STALE復旧

| No | 対象 | 前提条件 | 手順 | 期待結果 | 関連 |
|---|---|---|---|---|---|
| T-080 | 実行中情報の保存項目 | AI起動中 | `runtime/`の内容を確認 | PID・プロセス開始時刻・AI名・UID・依頼ID・元メールID・起動コマンド・記録日時が保存されている | SPEC.md 24章 |
| T-081 | PID再利用時の誤判定防止 | 保存PIDが別プロセスに再利用された状態を再現 | オーケストレーターを起動 | PID一致のみで実行中と判定せず、開始時刻の不一致からSTALEと判定する | SPEC.md 24章 |
| T-082 | STALE・未読メール残存時 | STALE検出、かつ元メールが未読 | オーケストレーターを起動 | 再処理候補として扱われる | SPEC.md 24章 |
| T-083 | STALE・既読かつ返信なし | STALE検出、既読で返信がない状態 | オーケストレーターを起動 | 元の送信者へ異常終了通知が送られる | SPEC.md 24章 |
| T-084 | STALE・返信済み | STALE検出、返信済みの状態 | オーケストレーターを起動 | 完了済みとして整理される | SPEC.md 24章 |
| T-084a | STALE・元メールID以下の古い返信の誤判定防止 | STALE検出、同じ依頼IDでメールIDが元メールID以下のメールが存在 | オーケストレーターを起動 | 返信とみなさず、完了済みと誤判定しない（`after_mail_id`により除外される） | SPEC.md 24章, 30章 |
| T-084c | STALE・起動日時より前に作成された返信の誤判定防止 | STALE検出、同じ依頼ID・送受信者UIDも一致し、**メールIDは元メールIDより大きいが、実行情報へ記録された起動日時より前に作成された**メールが存在 | オーケストレーターを起動 | 返信とみなさず、完了済みと誤判定しない。`after_mail_id`だけでは除外できないため、`sent_after`の指定漏れを検出できる | SPEC.md 24章, 30章 |
| T-084b | STALE・別送受信者メールの誤判定防止 | STALE検出、同じ依頼IDだが送信者UIDまたは受信者UIDが期待値と異なるメールが存在 | オーケストレーターを起動 | 返信とみなさず、完了済みと誤判定しない（返信判定条件は30章と同一） | SPEC.md 24章, 30章 |
| T-085 | 強制終了後の復旧 | オーケストレーターを強制終了した状態を再現 | 再起動する | 次回起動時に同じSTALE復旧処理が行われる | SPEC.md 24章 |
| T-086 | PC再起動後の状態確認 | PC再起動を再現 | オーケストレーターを起動 | SQLiteメール・引継ぎ情報・成果物から未完了作業を確認できる | SPEC.md 26章 |

## 9. 停止操作

| No | 対象 | 前提条件 | 手順 | 期待結果 | 関連 |
|---|---|---|---|---|---|
| T-090 | Ctrl+Cによる停止（AI非実行中） | 常時監視モードでAI非実行中 | Ctrl+Cを送信 | 新規AIを起動せず速やかに終了する | SPEC.md 25章 |
| T-091 | 停止要求（AI実行中、時間内終了） | 常時監視モードでAI実行中 | `stop.request`を作成 | 新規AIを起動せず、実行中AIの終了を待ってから終了し、`stop.request`が削除される | SPEC.md 25章 |
| T-092 | 停止要求（タイムアウト超過） | AI実行中でCLI実行タイムアウトを超過する状況 | `stop.request`を作成し待機 | プロセスを終了し、中断をログ・引継ぎへ記録、必要に応じ送信者へ通知してから終了する | SPEC.md 25章 |
| T-093 | 強制停止（二重Ctrl+C） | AI実行中に停止要求済み | 再度Ctrl+Cを送信 | 強制停止し、その事実をログ・引継ぎへ記録、送信者へ通知する | SPEC.md 25章 |

## 10. 文字コード

| No | 対象 | 前提条件 | 手順 | 期待結果 | 関連 |
|---|---|---|---|---|---|
| T-100 | 日本語メールの送受信 | 日本語件名・本文のメール | 送受信を実行 | 文字化けしない | SPEC.md 21章, 30章 |
| T-101 | CLI入出力のUTF-8 | 日本語を含む標準入出力 | CLIを起動し入出力を確認 | `PYTHONUTF8=1`等が設定され文字化けしない | SPEC.md 21章 |
| T-102 | ログ・設定・引継ぎファイルの文字コード | 日本語を含む各種ファイル | ファイルを生成・読込 | すべてUTF-8で文字化けなく扱われる | SPEC.md 21章 |

## 11. 初期動作確認シナリオ（SPEC.md 29章）

| No | 対象 | 前提条件 | 手順 | 期待結果 | 関連 |
|---|---|---|---|---|---|
| T-110 | 一連の往復シナリオ | AI未登録の初期状態 | SPEC.md 29章の15手順を通しで実行 | 登録→（手順2のみ人間が指揮AIを手動起動し指揮AIが依頼送信）→Claude Code起動→成果物/QandA作成→完了報告→Codex CLI起動→回答/修正指示→Claude Code再起動→反映→完了→未読ゼロで全AI終了、まで一貫して動作する | SPEC.md 29章, QandA Q011 |

## 12. ログ・秘密情報

| No | 対象 | 前提条件 | 手順 | 期待結果 | 関連 |
|---|---|---|---|---|---|
| T-120 | ログ記録項目 | AI起動・終了が発生 | ログファイルを確認 | 日時・依頼ID・メールID・起動AI・実行コマンド（秘密情報除く）・開始/終了時刻・終了コード・実行結果・作成/変更ファイル・エラー・次の宛先が記録されている | SPEC.md 23章 |
| T-121 | ログの秘密情報除外 | 同上 | ログファイルを確認 | APIキー・認証情報・Cookie・秘密の環境変数が記録されていない | SPEC.md 23章 |

## 13. mail単体動作の独立性

| No | 対象 | 前提条件 | 手順 | 期待結果 | 関連 |
|---|---|---|---|---|---|
| T-130 | orchestrator削除後のmail動作 | `orchestrator`フォルダを削除した状態 | `mail`パッケージ単体で送受信を実行 | `mail`単体のメール送受信機能が使用できる | SPEC.md 7章 |

## 14. CLI出力保存・利用制限引継ぎ

| No | 対象 | 前提条件 | 手順 | 期待結果 | 関連 |
|---|---|---|---|---|---|
| T-140 | stdout/stderr個別保存 | CLIが両ストリームへ出力 | AIを起動し終了させる | stdoutとstderrが別ファイルに保存され、相対パス・SHA-256・保存量・切捨て有無がJSONLへ記録される | FAILOVER_DESIGN.md |
| T-141 | 保存量上限とパイプdrain | 上限より大きいデータを高速出力するCLI | AIを起動し終了を待つ | ファイルは各上限以内、プロセスはパイプ詰まりなく終了し、切捨て有りになる | FAILOVER_DESIGN.md |
| T-142 | チャンク境界の秘密情報 | 秘密文字列を複数チャンクに分割して出力 | AIを起動しログ・ファイル・通知を確認 | 保存末尾・通知・ログに秘密情報がなく、マスキング済み値だけが残る | SPEC.md 23章, 30章 |
| T-143 | 通知末尾上限 | stdout/stderrに長いエラーを出力 | エラー通知を確認 | 各ストリームのマスキング済み末尾だけが設定バイト数以内で掲載され、全文は掲載されない | SPEC.md 30章 |
| T-144 | Claude利用制限判定 | Claudeアダプターが既知の制限文言を検出 | 非ゼロ終了CLIを起動 | `RATE_LIMITED`、アダプター名、規則ID、stream、短い根拠が記録される | SPEC.md 26章 |
| T-145 | 終了コードだけでは誤分類しない | 非ゼロ終了だが制限文言なし | CLIを起動 | `FAILED`になり、`RATE_LIMITED`にはならない | SPEC.md 26章 |
| T-146 | 代替AI選択 | `fallback_agents`に未訪問候補を複数設定 | RATE_LIMITEDを発生させる | 定義順の最初の候補へ同じ依頼IDのHANDOFFメールが送信され、担当履歴と回数が保存される | FAILOVER_DESIGN.md |
| T-147 | 同一AI再起動禁止 | 元AIがRATE_LIMITED | 監視サイクルを継続 | 元AIへの通常再試行が発生せず、代替AIだけが起動対象になる | SPEC.md 26章 |
| T-148 | 引継ぎ循環防止 | A→B→Aのfallback設定 | RATE_LIMITEDを連続発生させる | 訪問済みAを再選択せず、候補消尽後に`HUMAN_REQUIRED`になる | FAILOVER_DESIGN.md |
| T-149 | 引継ぎ上限 | `max_handoffs`を小さく設定 | RATE_LIMITEDを上限回数発生させる | 上限到達後は引継ぎせず、`HUMAN_REQUIRED`通知を送る | FAILOVER_DESIGN.md |
| T-150 | 全候補利用不能 | 代替候補が未登録、CLI起動不能、または送信失敗 | RATE_LIMITEDを発生させる | 依頼IDを維持した担当履歴を残し、元送信者へ`HUMAN_REQUIRED`通知を送る | SPEC.md 30章 |
| T-151 | 再起動途中の引継ぎ | `HANDOFF_PENDING`を保存してプロセス停止 | オーケストレーターを再起動 | 二重HANDOFFを作らず、送信済みなら`HANDOFF_SENT`から代替AI処理を再開する | FAILOVER_DESIGN.md |
