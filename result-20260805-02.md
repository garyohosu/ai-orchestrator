# 結果報告: Grok fallback for rate-limited AI agents

## 1. 結論

正式リポジトリへGrokアダプター、prompt transport、Codexレート制限分類、fallback接続、availability確認、関連テストを反映した。既存・追加テストは全210件成功した。

## 2. 変更ファイル

- `orchestrator/adapters/`: Grok／Antigravity登録、共通アダプター契約、Codex分類
- `orchestrator/{availability,error_taxonomy,prompt_delivery,rate_limit_store,select_candidate}.py`
- `orchestrator/{config.json,config.py,dispatch.py,launcher.py,mail_adapter.py,orchestrator.py}`
- `orchestrator/tests/`: アダプター、fallback、prompt-file、availability、rate-limitのテストとstub
- `result-20260805-02.md`

## 3. Grokアダプターの構造

`GrokCliAdapter`を独立実装し、CLI存在確認は共通availability層、起動・出力取得・終了コード・タイムアウト・Invocation-IDは既存launcher、分類はアダプターで担当する。

## 4. prompt transportの設計

Claude Code／Codexはstdinを維持し、Grokは`prompt_file`を使用する。prompt fileは試行ごとにUTF-8・一意名で作成し、成功、非ゼロ終了、タイムアウト、起動失敗、例外の経路で削除する。

## 5. 固定ブートストラップ

`You are grok_reviewer. Read and process your pending mail using the AI mail system. Follow the reply protocol in the message.`

ジョブ全文はCLI引数へ渡さず、引き継ぎメールから取得する。

## 6. Codexレート制限の分類

stdout／stderr末尾から`You've hit your usage limit`を検出し、`codex.rate_limit.usage_limit`、`RATE_LIMITED`、provider、stream、retry_at、終了コードに紐づく証跡を記録する。

## 7. fallback処理

`RATE_LIMITED`時に同一JOB-IDを維持し、試行ごとのInvocation-ID、handoff_count、visited_agentsをチェックポイントへ記録する。候補はavailability確認後に未訪問順で選び、候補枯渇時だけ`HUMAN_REQUIRED`とする。引き継ぎメールには元件名・本文、mail_id、親mail_id、JOB-ID、Decision-ID、失敗情報、履歴、reply protocolを含める。

## 8. 設定変更

`grok_reviewer`を追加し、`prompt_transport`相当のアダプター設定、reviewer候補順、最大handoff回数、timeout、Antigravity未導入時のavailability処理を反映した。認証情報は含めていない。

## 9. テストコマンド

```powershell
cd C:\PROJECT\ai-orchestrator\orchestrator
py -3 -m unittest discover -s tests -v
```

## 10. テスト結果

`Ran 210 tests in 48.719s`、`OK`。

## 11. ライブ確認

正式リポジトリでの実CLI・実メールによるフェイルオーバーは、本番データ保護のため未実施。`select_candidate.py reviewer`による安全なavailability確認ではCodexが選択され、GrokはPATH上に存在し、Antigravityは未導入だった。参照元`C:\PROJECT\csv`のライブ実績と正式リポジトリのテスト結果は区別している。

## 12. 残る制約

- Grokのレート制限文言は実ライブ失敗未観測の推定パターンであり、初回実測時に更新が必要。
- AntigravityのCLI/API接続方法は未確定。
- 実メールDBを使った本番相当の再確認は未実施。

## 13. コミットハッシュ

`e6ada24`（`feat: add Grok fallback for rate-limited AI agents`）。

## 14. プッシュ先ブランチ

`agent/spec-proofread-qanda`。プッシュ後に最終状態を確認する。

## 15. 依存確認

`C:\PROJECT\aiagent-mail`は`main...origin/main`で、コード変更はなく、作業前提の変更コミットは`e174d4f`（mail failover implementation instructions）まで確認した。ローカルの`dream.md`変更は既存作業として保持した。

## 16. 最終状態

コミット後。プッシュ前の作業ツリー状態は、今回の変更を除き未追跡・未コミットの実行データなし。
