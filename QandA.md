# QandA

SPEC.mdのレビューで確認が必要な事項を記録する。

## Q001

- 状態: ANSWERED
- 重要度: 高
- 質問者: Codex
- 対象ファイル: orchestrator/SPEC.md
- 質問内容: `mail`パッケージの`check_mail`、`receive_mail`の引数・戻り値・既読化の仕様は何か。未読メールを取得した時点で既読になるのか、処理成功後に明示的に既読化するのか。また、二重起動防止や異常終了時にメールを再処理できる仕組みはあるか。
- 推奨案: 取得と既読化を分離し、メールID単位で「取得中」「処理済み」「処理失敗」を追跡する。処理失敗時は再試行可能にする（正式回答により不採用）。
- 回答: 既存`mail`パッケージの仕様を正式採用する。`check_mail(uid)`は未読数を返すだけで既読化せず、`receive_mail(uid)`はメールID昇順で未読メールを返し、取得と同一トランザクション内で既読化する。初期版ではメール状態を追加せず、既読メールを自動的に未読へ戻さない。CLI起動失敗など、対象AIがまだ受信していないことが明らかな場合だけ設定回数まで再試行し、それ以外の異常終了では元メールの送信者へ`FAILED`、`TIMEOUT`、`NO_REPLY`、`DELIVERY_FAILED`のいずれかを通知する。二重起動は`runtime`の実行情報と実プロセス状態で防止し、再実行は同じ依頼IDの新しいメールで行う。

## Q002

- 状態: ANSWERED
- 重要度: 高
- 質問者: Codex
- 対象ファイル: orchestrator/SPEC.md
- 質問内容: `config.json`の配置場所と、`対象プロジェクト`の指定形式（省略時の値、相対パスの基準、存在しない場合の扱い）は何か。`orchestrator`フォルダをコピーした先で、プロジェクトルートをどのように確定するか。
- 推奨案: デフォルトを`orchestrator.py`の親ディレクトリの親（プロジェクトルート）とし、設定値はその配下の相対パスだけに制限する（絶対パスも許可する正式回答により一部修正）。
- 回答: `config.json`は`project-root/orchestrator/config.json`に置く。パスは`orchestrator.py`自身の場所を基準に解決し、既定の対象プロジェクトは`orchestrator.py`の親の親である。`project_path`を省略した場合は`project-root`を使用する。指定する場合はプロジェクトルート基準の相対パスを使用し、ルート外へ出る指定は拒否する。絶対パスも許可するが非推奨とする。対象パスが存在しない、またはディレクトリでない場合はAIを起動せず、元メール送信者へ`DELIVERY_FAILED`を通知する。

## Q003

- 状態: ANSWERED
- 重要度: 高
- 質問者: Codex
- 対象ファイル: orchestrator/SPEC.md
- 質問内容: 依頼IDの採番主体と一意性の保証方法は何か。複数の指揮AI、人間、再送処理が同時に`JOB000001`を作らないことをどう保証するか。再送時は同じ依頼IDを使う条件も明確か。
- 推奨案: メールシステムまたはオーケストレーターで採番し、SQLiteの一意制約またはUUIDを用いる。再送は同一作業の再開に限り同じ依頼ID、新規作業は新しい依頼IDとする。
- 回答: 依頼IDは作業依頼メールを最初に作成する側が生成する。自動生成形式は`JOB-{UTC日時}-{ランダム8桁}`とし、ランダム部分はUUIDから生成する。新規作業は新しい依頼ID、同じ依頼への回答・修正指示・QandA回答後の再開・失敗後の再送は同じ依頼ID、独立したやり直しは新しい依頼IDとする。オーケストレーターは同じ依頼IDのメールを同一作業の履歴として扱う。

## Q004

- 状態: ANSWERED
- 重要度: 高
- 質問者: Codex
- 対象ファイル: orchestrator/SPEC.md
- 質問内容: 「返信メール」の判定条件と待機時間は何か。返信先は元メールの送信者UIDで固定か、本文の指定を優先するか。依頼IDが同じ複数メールや、古い返信メールを誤認しないための条件は何か。
- 推奨案: `返信確認タイムアウト`を設定し、送信者UID・受信者UID・依頼ID・元メール処理開始時刻より後の作成時刻を必須条件とする。返信先は元メールの送信者UIDを既定とする。
- 回答: 返信確認タイムアウトを`config.json`で設定する。返信先は元メール送信者UIDを原則とし、本文に有効な返信先UIDがあれば優先する。返信判定には、処理対象AIの送信者UID、期待する返信先UID、同じ依頼ID、元メールIDより大きいメールID、CLI起動後の作成時刻を必須とする。CLI終了後もタイムアウトまでSQLiteを確認し、見つからなければ`NO_REPLY`を元メール送信者へ通知する。古い同一依頼IDメールは返信とみなさない。

## Q005

- 状態: ANSWERED
- 重要度: 高
- 質問者: Codex
- 対象ファイル: orchestrator/SPEC.md
- 質問内容: 最大往復回数、最大再試行回数、最大連続実行時間、返信確認タイムアウトの単位・既定値・数え方は何か。再試行と指揮AI・作業AI間の往復を同じカウンターで数えるのか。
- 推奨案: すべて秒または回数で明記し、依頼ID単位で「往復回数」と「CLI再試行回数」を分離する。既定値と上限値を設定ファイル仕様に記載する。
- 回答: 既定値は、メール確認間隔5秒、CLI実行タイムアウト1800秒、返信確認タイムアウト30秒、最大往復回数10回、最大再試行回数2回、最大連続実行時間14400秒とする。最大再試行回数は追加試行回数であり、既定値2回は最大3回の起動試行を意味する。往復回数と再試行回数は分離し、完了報告・エラー通知・受領通知は往復回数に含めない。常時監視モードの最大連続実行時間は0の場合だけ無期限とする。

## Q006

- 状態: ANSWERED
- 重要度: 高
- 質問者: Codex
- 対象ファイル: orchestrator/SPEC.md
- 質問内容: CLIの認証切れ、対象ファイル不足、破壊的操作の要求などを、終了コードや標準出力からどのように判定するか。AIが返信せず正常終了した場合と、処理対象がなく正常終了した場合をどう区別するか。
- 推奨案: CLI終了コードだけでなく、返信メールの有無と実行中に記録された状態を組み合わせる。判定できない場合は`HUMAN_REQUIRED`として停止する。
- 回答: CLIの起動可否、終了コード、タイムアウト、返信メールの有無・依頼ID・送受信者UID、AIが返信した状態を組み合わせて判定する。起動不能は`DELIVERY_FAILED`、0以外の終了や予期せぬ終了は`FAILED`、タイムアウトは`TIMEOUT`、終了コード0かつ返信なしは`NO_REPLY`とする。未読が0で起動しなかった場合は正常終了として`NO_WORK`をログに記録する。認証切れ等は、可能ならAI返信の状態を優先し、判定不能な場合は`HUMAN_REQUIRED`で停止する。標準出力・標準エラーだけで断定しない。

## Q007

- 状態: ANSWERED
- 重要度: 中
- 質問者: Codex
- 対象ファイル: orchestrator/SPEC.md
- 質問内容: 常時監視モードの停止方法、停止要求を検出する手段、AI実行中に停止要求を受けた場合の扱いは何か。
- 推奨案: `runtime/stop.request`などの明示的な停止要求ファイルと、Ctrl+Cによる安全な停止を用意し、実行中CLIは原則として終了を待ってから停止する。強制終了が必要な場合は引継ぎへ記録する。
- 回答: 常時監視モードはCtrl+Cまたは`orchestrator/runtime/stop.request`で停止する。停止要求後は新規AIを起動せず、実行中AIの終了を待つ。CLI実行タイムアウトを超えた場合はプロセスを終了し、中断をログ・引継ぎへ記録し、必要に応じて送信者へ通知する。その後オーケストレーターを終了し、処理済みの`stop.request`を削除する。再度Ctrl+Cを受けた場合は強制停止し、その事実を記録・通知する。

## Q008

- 状態: ANSWERED
- 重要度: 中
- 質問者: Codex
- 対象ファイル: orchestrator/SPEC.md
- 質問内容: AIを順番に起動する際の「登録順または設定順」は、どちらを正式仕様とするか。また、同じAIに複数の未読メールがある場合の処理順と、処理中に新着メールが届いた場合の扱いは何か。
- 推奨案: 設定ファイルの定義順を正式な優先順とし、各AIではメールIDの昇順で処理する。起動中の新着メールは同一プロセスで処理できる範囲に任せ、終了後に残件を再確認する。
- 回答: AIの処理順は`config.json`のエージェント定義順とし、登録UID順は使わない。同一AIの未読メールはメールID昇順で処理する。起動中の新着メールは同じプロセスで処理しても、終了後に再起動してもよいが、CLI実行タイムアウトを適用する。AI終了後は必ず未読数を再確認する。

## Q009

- 状態: ANSWERED
- 重要度: 中
- 質問者: Codex
- 対象ファイル: orchestrator/SPEC.md
- 質問内容: `runtime`に残った実行情報から、プロセスが存在しないと判断する方法と、プロセスIDの再利用を防ぐ方法は何か。オーケストレーター自身が強制終了した場合の復旧処理も必要か。
- 推奨案: PIDだけでなくプロセス開始時刻、AI名、依頼ID、起動時刻を保存し、PIDと開始時刻を照合する。孤立した実行情報は`STALE`としてログに記録し、未処理メールを再取得可能にする。
- 回答: `runtime`にはPID、プロセス開始時刻、AI名、AI UID、依頼ID、元メールID、起動コマンド、オーケストレーターが記録した起動日時を保存する。プロセス確認ではPIDと開始時刻を照合し、PIDだけで別プロセスを終了しない。対応プロセスがない実行情報は`STALE`として記録する。未読なら再処理候補、既読で返信なしなら異常終了通知、返信済みなら完了済みとして扱う。オーケストレーター強制終了後も次回起動時に同じ復旧処理を行う。

## Q010

- 状態: ANSWERED
- 重要度: 中
- 質問者: Codex
- 対象ファイル: orchestrator/SPEC.md
- 質問内容: エラー通知メールを送信できなかった場合の扱いは何か。また、元メールの送信者が存在しない・無効なUIDの場合、どこへ通知し、どの状態で停止するか。
- 推奨案: 通知送信自体の失敗をログと引継ぎへ記録し、`HUMAN_REQUIRED`で停止する。通知先が無効な場合も同じ扱いとし、エラー通知の再帰送信は行わない。
- 回答: エラー通知メールの送信に失敗した場合は再帰的な通知を作成せず、失敗をログと引継ぎへ記録し、対象依頼を`HUMAN_REQUIRED`として自動処理を停止する。無効な送信者UIDの場合も同じ扱いとし、通知先を別AIへ変更しない。オーケストレーター自身への通知も作成しない。
