## 2026-07-30 16:20 Dreamingタイム

### 今回やったこと
- `orchestrator/SPEC.md`（958行、既にQandA.mdで10件全てANSWERED済みの成熟した仕様）を起点に、`/spec-to-design`スキルでUSECASE.md/SEQUENCE.md/CLASS.md/UI.md/TESTCASE.mdの5点セットを新規作成
- 各成果物をCodex CLIで個別レビュー（USECASE.mdは丁寧レビュー、以降は軽量レビュー）し、指摘を都度反映してからAPPROVEDを得た

### 気づいたこと
- SPEC.mdが非常に詳細（返信確認、STALE復旧、エラー通知の再帰防止など）だったため、USECASE/SEQUENCE/CLASS/TESTCASEの各段階で「既読化済みメールの再試行禁止」「HUMAN_REQUIREDの終端状態」「エラー通知必須項目」など、細部の見落としを毎回1〜2周の指摘で拾われた
- Codexレビューは「起動失敗（未受信）」と「起動成功後の異常終了/タイムアウト（受信済み扱い）」の区別を特に重視しており、この境界線がSPEC全体を貫く最重要ルールだった

### 改善点
- 初回生成時点で「未受信 vs 既読後」の区別をテストケース・クラス設計の両方で最初から明示しておけば、レビュー往復を1回減らせた可能性がある
- QandA.mdがプロジェクトルート（orchestrator配下ではない）にある点は、SPEC.mdのフォルダ構成通りだが見落としやすいので次回も先に場所を確認する

### 次に試すとよさそうなこと
- 実装フェーズ（orchestrator.py等の実コード）に進む際は、CLASS.mdの`OutcomeClassifier`と`RetryPolicy`の境界（mail_received判定）を最初にユニットテスト化すると手戻りが少なそう
- TESTCASE.mdのT-110（初期動作確認15手順の通しシナリオ）を最初の統合テストとして実装すると、他の個別テストの前提条件（AI登録・メール送受信）を自然に満たせる

## 2026-07-31 10:04 Dreamingタイム

### 今回やったこと
- `orchestrator` production codeをSPEC・設計資料・QandA Q011〜Q013と照合してレビュー
- Windowsプロセス制御、メール既読化、返信照合、通知、再試行、STALE復旧、秘密情報、コピー可能性を確認

### 気づいたこと
- 単体テストが通っていても、起動時エラー・プロセス終了競合・通知メールの再入力・依頼ID由来パスなど、統合時の境界条件に重大な問題が残る
- `find_mails`経路と`receive_mail`非使用の設計は守られている

### 改善点
- PIDと開始時刻の検証を、起動直後の保存・終了処理・強制終了まで一貫させる必要がある
- 終端状態をメールIDだけでなく、通知メールを再処理しない仕組みと永続的な往復管理まで含めて設計する必要がある

### 次に試すとよさそうなこと
- 指摘事項を修正し、実`mail`パッケージとの統合試験、再起動試験、PID再利用・taskkill失敗試験を追加する

## 2026-07-31 10:25 Dreamingタイム

### 今回やったこと
- `orchestrator`の修正8件と反証2件を実装・テスト・設計経路に照らしてレビュー
- パス防御、PID再検証、終了確認、返信時刻境界、通知再帰停止、秘密情報マスキング、往復回数永続化を確認

### 気づいたこと
- 反証したproject_pathと自己送信通知の主張は、現コードの制御フローと整合する
- テスト実行環境では一時ディレクトリ権限とtaskkill制約により、111件の全PASSを独立再現できなかった

### 改善点
- `round_trips.json`のJSON型・値検証を追加すると、破損した永続状態に対してより堅牢になる

### 次に試すとよさそうなこと
- 実機Windows環境で、taskkill失敗・PID再利用・再起動後のround-trip上限を追加検証する

## 2026-07-31 10:42 Dreamingタイム

### 今回やったこと
- 最新コミットをpullし、Claude CLIの存在・バージョン・ヘルプをPowerShellで確認
- 既存実装とconfig.jsonを確認し、Claudeは`-p`＋標準入力で非対話起動する構成だと整理
- 実プロンプト実行は行わず、既存テスト111件を実行して全件PASSを確認

### 気づいたこと
- Claude CLIは`C:\Users\hantani\.local\bin\claude.exe`、2.1.220で利用可能だった
- 利用可能な非対話フラグは`-p/--print`、入力形式は`text`（既定）または`stream-json`
- 現行config.jsonの`command: []`はPATH解決により動作し、追加設定なしで最小要件を満たす

### 改善点
- CLIの非ゼロ終了をRATE_LIMITED等へ細分類する機能は未実装で、今回の調査範囲では変更していない
- 実プロンプトによる認証・権限・標準入力の実機確認は未実施

### 次に試すとよさそうなこと
- 利用制限解除後、`claude -p`へ標準入力で安全な短いプロンプトを渡し、終了コード・stdout/stderr・権限動作を確認する
- 必要なら`agents[].command`へ絶対パスを設定し、サービス環境でもPATH差異を避ける

## 2026-07-31 10:52 Dreamingタイム

### 今回やったこと
- CLI stdout/stderr保存、上限、マスキング、SHA-256、RATE_LIMITED分類、代替AI引継ぎの設計を作成
- `SPEC.md`、`USECASE.md`、`SEQUENCE.md`、`CLASS.md`、`UI.md`、`TESTCASE.md`、`QandA.md`へ整合する設計差分を反映
- コード、config.json、コミット、pushは変更せず、Q014へCLI固有の未確定判定文言を記録

### 気づいたこと
- 出力保存はマスキング済みデータをストリーミング書込みし、上限後も読み捨てる設計で、秘密漏えいとパイプ詰まりを同時に防ぐ必要がある
- RATE_LIMITEDは終了コードではなくCLIアダプターの根拠付き判定として扱い、依頼ID単位の担当履歴で循環を防ぐ必要がある

### 改善点
- 実装前に各CLIの実機出力サンプルを収集し、アダプターの判定規則を確定する必要がある

### 次に試すとよさそうなこと
- 設計承認後、OutputPumpの大容量・チャンク境界・秘密情報マスキング試験から実装する
- その後、RATE_LIMITEDの代替AI選択、再起動復旧、候補全滅の統合試験を追加する

## 2026-07-31 11:13 Dreamingタイム

### 今回やったこと
- stdout/stderrの個別pipe逐次読取、1 MiB証跡保存、64 KiB末尾リング、8 KiB通知末尾、マスキング、SHA-256、JSONL記録を実装
- Claude Code 2.1.220の`You've hit your session limit`を`RATE_LIMITED`判定し、`temporarily unavailable`は`FAILED`扱いとしてQ014へ反映
- fallback_agentsによる依頼ID維持の引継ぎ、担当履歴、回数、循環・二重送信防止、候補なし時のHUMAN_REQUIRED遷移を実装
- 大容量スタブ、秘密情報、Claude判定、引継ぎ重複防止の追加テストを実装

### 気づいたこと
- 保存上限と判定用リングを分けることで、証跡ファイルを増やさず終了直前の利用制限メッセージを検出できる
- 実mailパッケージを同梱したコピー環境でも`--once`が正常起動し、対象なしで終了できた

### 改善点
- CodexおよびClaude Code 2.1.220以外の実機文言は未確認であり、未知の文言をRATE_LIMITEDへ推測分類しない
- 実mailでの実依頼往復、実プロンプト、認証・課金を伴う試験は未実施

### 次に試すとよさそうなこと
- 利用制限解除後、追加の実機メッセージを秘密情報除去済みで収集し、CLIアダプターの規則を必要に応じて拡張する
- 実運用前に複数候補のCLI起動不能・送信失敗・再起動途中をmail統合環境で検証する
